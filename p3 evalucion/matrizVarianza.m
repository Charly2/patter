function [MVar] = matrizVarianza(numC,numE,clases,medias)
    l = length (Clases(:,1));
    contador = 1;
    for i = 1:numC
        c = []; %se guarda la clase
       for j=1:numE
           c = [clases(contador,:)];
           contador = contador + 1;
       end
       
    end
end